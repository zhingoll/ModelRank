# Shared results

This directory is reserved for large shared result files used by multiple scripts.

The most important file is:

- `modelrank_scores.csv`

This file is intentionally not tracked in the main Git repository because of its size. It should be distributed as a GitHub Release asset and placed in this directory before running scripts that require it.
