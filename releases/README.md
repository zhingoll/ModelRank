# Release assets

This directory documents large files that should be attached as GitHub Release assets rather than committed to the main repository.

At minimum, the following file is expected:

- `modelrank_scores.csv`

Additional processed monthly inputs may also be attached as Release assets if full script reruns are required.

The main Git repository is intentionally kept lightweight and only contains the scripts and result tables that directly support the reported paper results.
